<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SET_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyAPP"
        tools:replace="android:theme">
        <activity android:name=".SetReminder"></activity>
        <activity android:name=".RemindersMain" />
        <activity android:name=".NewBills" />
        <activity android:name=".Bills" />
        <activity android:name=".Newhome" />
        <activity android:name=".Home" />
        <activity android:name=".Groceries" />
        <activity android:name=".singoutdumb" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Signoutfragment" />
        <activity android:name=".NewNoteActivity" />
        <activity android:name=".MainNotes" />
        <activity android:name=".PhoneNumber" />
        <activity android:name=".VerifyPhoneActivity" />
        <activity android:name=".TitlePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" />
        <activity android:name=".Login" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <receiver android:name=".Notification_receiver"/>
        <receiver android:name=".Notification_receiver1"/>
    </application>

</manifest>